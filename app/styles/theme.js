import themeData from './theme.json'

export const breakpoints = themeData.breakpoints
export const transitions = themeData.transitions
export const heights = themeData.heights
export const spacing = themeData.spacing
export const colors = themeData.colors
export const fonts = themeData.fonts
export const shared = themeData.shared

export default themeData

